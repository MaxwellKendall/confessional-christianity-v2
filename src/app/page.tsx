import { HomeClient } from '@/components/HomeClient';

// The homepage's job is getting a family into worship or today's question
// (PRD §8) — no account, no held screen, two cards in.
export default function Home() {
  return <HomeClient />;
}
